(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-server()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-deploy()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-render()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-migrate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-clean()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-list()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-version()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-new()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))

(defun hexo-generate()
  (interactive)
  (shell-command "hexo gen"))
